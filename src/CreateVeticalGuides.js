// function createVeticalGuides() {
//
// 	var doc = context.document,
// 			selection = context.selection,
// 			textLayer = selection.TextLayer(),
// 		sketch = content.api(),
// 			pages = [[doc currentPage] currentArtboard] || [doc currentPage];
// }
